name: Fetch translation runtime

on:
  push:
    paths:
      - '.github/workflows/fetch-translation-runtime.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download CTranslate2 wheel and full EN-ZH model
        shell: bash
        run: |
          set -euxo pipefail
          rm -rf runtime
          mkdir -p runtime/model
          curl -L --fail --retry 5 \
            'https://files.pythonhosted.org/packages/77/6d/131193b68d3884f9ab9474d916c6244df2914fbb3234d2a4c1fada72b1d6/ctranslate2-4.6.0-cp313-cp313-manylinux_2_17_x86_64.manylinux2014_x86_64.whl' \
            -o runtime/ctranslate2-4.6.0-cp313-cp313-manylinux_2_17_x86_64.manylinux2014_x86_64.whl
          base='https://huggingface.co/manancode/opus-mt-en-zh-ctranslate2-android/resolve/main'
          for f in config.json model.bin shared_vocabulary.json source.spm target.spm tokenizer_config.json vocab.json; do
            curl -L --fail --retry 5 "$base/$f?download=true" -o "runtime/model/$f"
          done
          sha256sum runtime/* runtime/model/* || true
          du -sh runtime

      - name: Upload translation runtime
        id: upload
        uses: actions/upload-artifact@v4
        with:
          name: translation-runtime-full
          path: runtime
          if-no-files-found: error
          retention-days: 1
          compression-level: 6

      - name: Publish artifact metadata
        shell: bash
        run: |
          set -euxo pipefail
          cat > runtime-artifact.json <<EOF
          {
            "run_id": "${GITHUB_RUN_ID}",
            "artifact_id": "${{ steps.upload.outputs.artifact-id }}",
            "artifact_url": "${{ steps.upload.outputs.artifact-url }}",
            "artifact_name": "translation-runtime-full",
            "updated_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          }
          EOF
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add runtime-artifact.json
          git commit -m 'Publish full translation runtime artifact metadata' || true
          git push
